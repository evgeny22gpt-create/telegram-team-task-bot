# Проверка локального ключа Google Service Account

Дата проверки: 2026-07-12 (МСК).

## Что настроено

- Для сервисного аккаунта `tgbot1` создан JSON-ключ.
- Ключ сохранён локально как `credentials/google-service-account.json`.
- В `.env` указаны ID таблицы, имя листа `Задачи` и путь к файлу ключа.
- Содержимое закрытого ключа не выводилось и не копировалось в документацию.

## Проверки безопасности

- JSON успешно разобран.
- Тип учётных данных: `service_account`.
- Идентификатор проекта и email сервисного аккаунта соответствуют проекту.
- Правило `credentials/*.json` исключает JSON-ключи из Git.
- Правило `.env` исключает локальные переменные и токен из Git.

## Проверки проекта

- `config.py` сообщает `google_sync_configured = True`.
- Настройки таблицы и путь к файлу ключа распознаются корректно.
- Тесты запущены через локальный `.venv`: 20 из 20 пройдены.

Следующий этап: установить библиотеку для Google Sheets API и выполнить безопасное чтение тестового диапазона таблицы.
