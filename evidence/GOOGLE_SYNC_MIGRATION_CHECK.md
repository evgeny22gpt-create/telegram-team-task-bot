# Проверка миграции SQLite для Google-синхронизации

Дата проверки: 2026-07-12 00:47 МСК.

## Что изменено

К исходной таблице `tasks` добавлены три поля:

- `status` — состояние задачи;
- `category` — категория задачи;
- `updated_at` — время последнего изменения для будущего разрешения конфликтов синхронизации.

## Резервная копия

- Файл: `backups/tasks_before_google_sync_migration_20260712_004200.db`
- SHA-256 рабочей базы до миграции: `BB665C682D9815CCBE3B557AB068805272B05B11B58C57F7E6A0D34497C2EB53`
- SHA-256 резервной копии: `BB665C682D9815CCBE3B557AB068805272B05B11B58C57F7E6A0D34497C2EB53`

Совпадение хешей подтверждает, что резервная копия побайтно равна базе до изменения.

## Результат проверки

- Схема до: `id, text, user, created_at`.
- Схема после: `id, text, user, created_at, status, category, updated_at`.
- Строк до миграции: 3.
- Строк после миграции: 3.
- Исходные поля `id`, `text`, `user`, `created_at` совпали с резервной копией: да.
- Для старых записей `status`: `Новая`.
- Для старых записей `category`: `Без категории`.
- Для старых записей `updated_at` совпадает с `created_at`: да.
- `PRAGMA integrity_check` до миграции: `ok`.
- `PRAGMA integrity_check` после миграции: `ok`.
- SHA-256 базы после миграции: `754E08A98C487047BD47CF279821B42D97C1536BC5AF27410A3243A9D5CBD430`.

Изменение хеша после миграции ожидаемо: структура файла SQLite была расширена.

## Автоматические проверки

- Модульные тесты: 18 из 18 пройдены.
- Проверка синтаксиса `py_compile`: пройдена.
- Тест миграции дважды применяет обновление к временной старой базе и подтверждает сохранность данных.
